//LISTA DE METAS
let metas = ['acordar', 'trabalhar', 'malhar', 'estudar']
console.log(metas);

//OBJETO - chave/propiedade e valor
let meta = {
    value: 'ler um livro por mês',
    checked: true,
    log: (info) => {
        console.log(info)
    }
}

meta.log(meta.value)

//FUNÇÃO
const criarMeta = () =>{

}